# v2ray_scan
this project will scan your v2ray configs and give you a list of the ones that work.
its my first project and i will use AI in most of it parts so it could be buggy just like me life :D.
**this code can only scan raw config links for now**
## Installation
1. Clone the repository:
```bash
   git clone https://github.com/amirhab0/v2ray_scan.git

2. goto project folder:
```bash
    cd v2ray_scan

3. install required python library:
```
    pip install aiohttp